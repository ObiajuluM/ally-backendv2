TODO
https://geojson.io/next/
https://2kmfromhome.com/10km
https://nominatim.openstreetmap.org/search?q=Anambra+State&format=geojson&polygon_geojson=1
https://overpass-turbo.eu/?utm_source=chatgpt.com

USE: PostGIS FOR OPTIMIZATION
        Right now distance calculations happen in Python loops.
        With many responders this becomes slow.
        PostGIS would allow:
        polygon containment
        distance calculations
        nearest-neighbor queries
        directly in SQL.
        Huge performance boost.


# Production mode (DEBUG=False uses Postgres)
# Edit .env: set DEBUG=False + fill in DB_NAME/USER/PASSWORD
docker compose up --build

docker compose build --no-cache && docker compose up


# Create superuser
docker compose exec web python manage.py createsuperuser


# Basic — 10 users, 100 responders
docker compose exec web python manage.py seed

# Custom counts
docker compose exec web python manage.py seed --users 20 --responders 50

# Cluster responders around a specific location
docker compose exec web python manage.py seed --cluster-lat 4.8948 --cluster-lng 6.9719

# Repeatable data (same output every time)
docker compose exec web python manage.py seed --seed 42


Available Models
Gemini 3 Series - Latest experimental models with state-of-the-art reasoning:

gemini-3-pro-preview
gemini-3-flash-preview
gemini-3-pro-image-preview
Gemini 2.5 Series - Latest stable models:

gemini-2.5-pro
gemini-2.5-flash
gemini-2.5-flash-lite
Gemma 3 Series - Open models:

gemma-3-27b-it
gemma-3-12b-it
gemma-3-4b-it
gemma-3-1b-it





TODO:

1. Log in to Docker Hub
docker login

2. Build and tag the image
docker build -t <your-username>/ally-backend:latest .

3. Push to Docker Hub
docker push <your-username>/ally-backend:latest

One security note: your .env file contains real secrets (API keys, DB password,

